#include <stdio.h>
#pragma warning(disable:4996)
#include <stdlib.h>
#include <string.h>

int delete(int array[], int loc) {
	int i;
	for ( i = loc; i < n; i++)
	{
		array[i] = array[i + 1];
	}
	array[n - 1] = null;
	items--;
}
int main()
{
	complex c1;
	complex c2;

}