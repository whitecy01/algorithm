#include <stdio.h>
#pragma warning(disable:4996)
#include <stdlib.h>
#include <string.h>

int main()
{
	int two[10];
	int i;
	int squared;
	int number = 2;

	printf("--");
	for ( i = 0; i < 10; i++)
	{
		if (i = 0)
		{
			number = 1;
		}
		number *= 2;

	}

	printf("%d", number);
}